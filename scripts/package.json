{
  "name": "git-host-rn-web-before-after-scripts",
  "private": true,
  "version": "1.0.2",
  "description": "Minimal screenshot capture helper for GitHub/GitLab RN web before/after workflow",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "setup": "npm install",
    "browser:install": "pnpm dlx playwright install chromium",
    "browser:path": "node -e \"const fs=require('node:fs');const path=require('node:path');const home=process.env.HOME||'';const base=path.join(home,'Library/Caches/ms-playwright');if(!fs.existsSync(base)){process.exit(1);}const dirs=fs.readdirSync(base).filter((d)=>d.startsWith('chromium-')).sort((a,b)=>Number(b.split('-')[1])-Number(a.split('-')[1]));for(const dir of dirs){const root=path.join(base,dir);const candidates=[path.join(root,'chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing'),path.join(root,'chrome-linux/chrome'),path.join(root,'chrome-win/chrome.exe')];const found=candidates.find((candidate)=>fs.existsSync(candidate));if(found){console.log(found);process.exit(0);}}process.exit(1);\"",
    "help": "bash ./screenshot-before-after.sh --help"
  },
  "dependencies": {
    "minimist": "^1.2.8",
    "puppeteer-core": "^24.8.2"
  }
}
